#include <stdio.h> 
#include <stdlib.h> 
int main() { 

    int valor_inteiro; 
    valor_inteiro = 2.97; 
    printf("O valor armazenado eh: %d\n", valor_inteiro); 
    
    system("PAUSE"); 
    
    return 0; 
} 

a) Qual é o valor numérico que será efetivamente exibido no console ao executar esse 
programa? 
r = 2 

b) Explique por que isso ocorre. Qual é o nome do fenômeno que acontece nessa atribuição? 
r = Isso ocorre porque a variavel foi declarada como int, e int nao guarda casas decimais. Quando atribuimos 2.97 (que e float) a ela, o C corta a parte decimal sem arredondar. Esse fenomeno se chama truncamento.

c) Como este tipo de comportamento pode ser evitado ou controlado explicitamente em C pelo 
programador caso ele necessite arredondar o valor ou manter a precisão?
r = Para evitar isso e manter a precisao, basta declarar a variavel como float ou double em vez de int.