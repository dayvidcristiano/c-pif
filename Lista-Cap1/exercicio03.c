#include <stdio.h>

int main() {

    /*Apenas testando minhas habilidades de comentar um código*/
    int n = 25; /*Declarando um variável que armazena um número inteiro*/
    printf("%d", n); /*Imprimindo o número armazenado na variável*/

    return 0;
}